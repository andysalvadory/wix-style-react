const prefix = 'selectable-accordion';

export const dataHooks = {
  item: `${prefix}-item`,
  itemHeader: `${prefix}-item-header`,
};

export const TYPE = {
  RADIO: 'radio',
  CHECKBOX: 'checkbox',
  TOGGLE: 'toggle',
};
